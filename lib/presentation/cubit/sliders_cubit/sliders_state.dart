part of 'sliders_cubit.dart';

@immutable
class SlidersState {
  final bool isLeftSliderEnabled;

  final bool isRightSliderEnabled;

  const SlidersState(this.isLeftSliderEnabled, this.isRightSliderEnabled);
}
