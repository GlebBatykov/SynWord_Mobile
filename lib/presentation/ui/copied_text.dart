import 'package:flutter/widgets.dart';

class CopiedText extends StatelessWidget {
  const CopiedText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
