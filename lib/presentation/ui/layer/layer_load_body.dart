import 'package:flutter/material.dart';

class LayerLoadBody extends StatelessWidget {
  const LayerLoadBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
